#include "当康.h"

引入 当康::基础类;
引入 当康::包装类;
引入 当康::开发者;

命名空间 当康 {
命名空间 图形处理 {

类定义(当康::图形处理::画笔)

画笔& 画笔::构造() {
	返回 本体;
}

void 画笔::析构() {

}

画笔& 画笔::构造(只读 画布 &现有画布) {
	设置画布(现有画布);
	返回 本体;
}

画笔& 画笔::复制构造(只读 画笔 &其他实例) {
	返回 本体;
}

画笔& 画笔::移动构造(画笔 &&其他实例) {
	返回 本体;
}

只读 画布& 画笔::获取画布() 只读 {
	返回 *持有画布;
}

void 画笔::设置画布(只读 画布 &现有画布) {
	持有画布 = 创建 画布(现有画布);
}

只读 颜色& 画笔::获取画笔颜色() 只读 {
	返回 当前颜色;
}

void 画笔::设置画笔颜色(只读 颜色 &颜色) {
	当前颜色 = 颜色;
}

}
}


